services:
  - type: web
    name: scholi-backend
    env: docker
    branch: main
    dockerfilePath: school_calendar_nest_prisma/Dockerfile
    envVars:
      - key: DATABASE_URL
        value: "postgres://user:password@localhost:5432/demo"
      - key: JWT_SECRET
        value: "replace_me"
    repo: https://github.com/mejakivos/scholi-demo

  - type: web
    name: scholi-frontend
    env: static
    branch: main
    buildCommand: npm install && npm run build
    staticPublishPath: school_calendar_deliverables/frontend/dist
    envVars:
      - key: REACT_APP_API_URL
        value: "/"
    repo: https://github.com/mejakivos/scholi-demo






